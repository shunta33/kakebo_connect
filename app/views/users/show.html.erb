<h2>マイページ</h2>
<h4>登録情報</h4>
<%= link_to "メールアドレス・パスワードを変更", edit_user_registration_path %>
<%= link_to "プロフィールを変更", edit_user_path(@user.id) %>
<table>
  <tr>
    <td>ハンドルネーム</td>
    <td><%= @user.name %></td>
  </tr>
  <tr>
    <td>1カ月の目標支出額</td>
    <td>
      <%= if @user.target_expenses.blank?
            p "未設定"
          else
            p @user.target_expenses
          end %>
    </td>
  </tr>
  <tr>
    <td>コメント</td>
    <td>
      <%= if @user.comment.blank?
            p "未設定"
          else
            p @user.comment
          end %>
    </td>
  </tr>
</table>